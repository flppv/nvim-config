require("onedark").setup({
  style = "deep",
  transparent = true,
  term_colors = true,
  code_style = {
    comments = "italic",
    strings = "italic",
  },
  lualine = {
    transparent = true,
  },
})

require("onedark").load()
