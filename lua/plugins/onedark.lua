require("onedark").setup({
  style = "deep",
  transparent = true,
  term_colors = false,
  code_style = {
    comments = "italic",
    strings = "italic",
  },
  lualine = {
    transparent = true,
  },
})

require("onedark").load()
